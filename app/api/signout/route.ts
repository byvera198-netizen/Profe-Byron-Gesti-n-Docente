import {cookies} from 'next/headers';
import {hashToken,run} from '../../../lib/server';
export async function GET(req:Request){const token=(await cookies()).get('byron_session')?.value;if(token)await run('DELETE FROM sessions WHERE id=?',await hashToken(token));const secure=new URL(req.url).protocol==='https:'?'; Secure':'';const headers=new Headers({Location:'/acceso'});headers.append('Set-Cookie','byron_session=; HttpOnly; SameSite=Lax; Path=/; Max-Age=0'+secure);headers.append('Set-Cookie','byron_signed_out=1; HttpOnly; SameSite=Lax; Path=/; Max-Age=31536000'+secure);return new Response(null,{status:302,headers});}
