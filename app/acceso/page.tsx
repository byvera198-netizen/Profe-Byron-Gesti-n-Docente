'use client';
import {useState} from 'react';

export default function Acceso(){const [mode,setMode]=useState<'login'|'register'>('login'),[busy,setBusy]=useState(false),[error,setError]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),payload={name:f.get('name'),email:f.get('email'),password:f.get('password')};setBusy(true);setError('');try{const r=await fetch('/api/auth/'+(mode==='login'?'login':'register'),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)}),b:any=await r.json();if(!r.ok)throw new Error(b.error||'No se pudo iniciar sesión.');location.assign('/');}catch(e:any){setError(e.message);}finally{setBusy(false);}}return <main className="access-screen"><section className="access-card"><a className="brand" href="/"><div className="brand-mark">B<span/></div><div><strong>Profe. Byron<span>GESTIÓN DOCENTE</span></strong></div></a><div className="access-copy"><div className="eyebrow">ACCESO INSTITUCIONAL</div><h1>{mode==='login'?'Ingresa a tu espacio':'Crea tu cuenta docente'}</h1><p>{mode==='login'?'Usa tu correo institucional o tu cuenta de Google.':'Regístrate con tu correo; después podrás solicitar acceso como docente o directivo a una institución existente.'}</p></div><div className="access-switch"><button type="button" className={mode==='login'?'active':''} onClick={()=>setMode('login')}>Ingresar</button><button type="button" className={mode==='register'?'active':''} onClick={()=>setMode('register')}>Crear cuenta</button></div><form className="access-form" onSubmit={submit}>{mode==='register'&&<label>Nombre completo<input name="name" required minLength={2} autoComplete="name" placeholder="Nombres y apellidos"/></label>}<label>Correo electrónico<input name="email" required type="email" autoComplete="email" placeholder="docente@institucion.edu.ec"/></label><label>Contraseña<input name="password" required type="password" minLength={10} autoComplete={mode==='login'?'current-password':'new-password'} placeholder="Mínimo 10 caracteres"/></label>{error&&<p className="error-text">{error}</p>}<button className="btn primary" disabled={busy}>{busy?'Verificando…':mode==='login'?'Ingresar con correo':'Crear mi cuenta'}</button></form><div className="access-divider"><span>o</span></div><a className="btn google-login" href="/api/google/start" target="_top">Continuar con Google</a><small>La primera persona que cree una unidad educativa será su administrador. Podrá registrar, aprobar y administrar a los docentes de su centro educativo.</small></section></main>}
